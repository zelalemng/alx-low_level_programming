#include <stdio.h>
/**
 * main - Entry points
 *
 * Return: Always 0 (success)
 */
int main(void)
{
	int z;

	z = 0;
	while
		(z < 10) {
			putchar(z + '0');
			z++;

		}
	putchar('\n');
	return (0);
}
